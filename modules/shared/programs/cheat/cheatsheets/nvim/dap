---
tags: [ neovim, lazyvim, dap, debug, javascript, typescript, nodejs ]
---
nvim-dap: JS/TS 서버사이드 디버깅 (Node/Next.js)

  사전: dap.core extra + Nix vscode-js-debug(js-debug-adapter wrapper). Linux(NixOS) 전용.
        (macOS는 VSCode로 디버깅 — 어댑터 바이너리 미설치)

디버깅 흐름 (이미 떠 있는 서버에 attach)

  1. 프로젝트에서 inspector 켜고 dev 서버 실행:
       node --inspect=127.0.0.1:9229 ...
       # Next.js: package.json 에 debug script 를 두고 실행한다. NODE_OPTIONS 를
       #   `pnpm dev` 등 패키지매니저 wrapper 앞에 붙이면 inspector 가 wrapper
       #   프로세스에 붙거나 9229 포트 충돌로 실패하므로 next 를 직접 띄운다:
       #     "dev:debug": "NODE_OPTIONS='--inspect=127.0.0.1:9229' next dev"
       #   → pnpm dev:debug  (또는 셸에서 NODE_OPTIONS=... next dev 직접 실행)
  2. nvim에서 <leader>db 로 breakpoint 설정
  3. <leader>dc (continue) → "Attach to :9229 (--inspect)" 선택
  4. 실제 요청을 보내 해당 코드 경로 실행 → breakpoint 정지

  보안: inspector는 반드시 127.0.0.1 에 바인딩 (0.0.0.0 은 tailnet 전체 노출)

주요 키맵 (LazyVim dap.core)

  <leader>db    breakpoint 토글
  <leader>dB    조건부 breakpoint
  <leader>dc    continue / 디버깅 시작 (config 선택)
  <leader>di    step into
  <leader>dO    step over
  <leader>do    step out
  <leader>dt    terminate
  <leader>du    DAP UI 토글
  <leader>de    선택 표현식 eval (normal/visual)
  <leader>dr    REPL 토글

함정 (Next.js App Router / turbopack)

  - runtime="edge" 코드는 Node inspector에 안 잡힘 (Node runtime만 가능)
  - 정적/캐시된 경로는 재요청해도 서버 코드가 재실행 안 될 수 있음
      → route handler에 임시 `debugger;` 를 넣고 실제 요청으로 먼저 증명
  - SSR은 child process로 실행됨 → attach config의 autoAttachChildProcesses=true 필요 (설정됨)
